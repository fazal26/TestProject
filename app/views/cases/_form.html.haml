= simple_form_for @case,:html => {:multipart => true } do |f|
  = select_tag(:category_id, options_for_select(@categories), :prompt => "Select a Category")
  = f.input :description, label: "Details Regargind Case"
  = f.button :submit
